pipeline {
    agent any
    
    stages {
                
        stage('Build') {
            steps {
                    echo 'building the application'
            }
        }
        stage('test') {
            steps {
                    echo 'Testing the application'
            }
        }
                        
        stage('Deploy') {
            steps {
                // Deploy your application
                // You can use tools like Docker, Ansible, etc.
              echo 'Deploying the application'
            }
        }
    }
}
    
